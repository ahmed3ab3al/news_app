import 'package:flutter/cupertino.dart';

class BusinessScreenView extends StatelessWidget {
  const BusinessScreenView({super.key});


  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Business Screen',
      ),
    );
  }
}
